# Progress

Current phase: v3 audit remediation — P1/P2/P3 fixed locally; durable release commit, re-audit, and production migration/deploy pending

## Phase 1: Scaffold + data layer — audited-pass
Notes: Next.js+Tailwind scaffold, Supabase schema (autoshop_services, autoshop_jobs, autoshop_status_history, autoshop_messages — prefixed, see decision log), idempotent seed script w/ RapidFix data (10 services, 5 jobs), nav + /services + /jobs list pages. Build, seed, and live render all verified. Added tests/seed.test.ts (node:test) asserting seeded services/jobs are queryable; wired to `npm test`.
Audit: 2026-07-14 — re-audit: audited-pass. tests/seed.test.ts wired via npm test, npm test passes 2/2 against live Supabase; npm run seed and npm run build re-verified clean. All Phase 1 "Done when" criteria and RULES.md rule 5 satisfied.

## Phase 2: Customer intake + estimate — audited-pass
Notes: Chat UI at /intake (Taglish), app/api/triage route calling OpenRouter (OPENROUTER_MODEL). Model only picks probable_issue/urgency/service_name; server grounds estimate_min/max by matching service_name against live autoshop_services rows (rule 9 — no invented numbers), returns needs_inspection if no match. Every result carries "Initial estimate, subject to inspection." disclaimer. callWithRetry() wraps the OpenRouter fetch. "Get my estimate now" forces a final answer for deterministic testing. tests/triage.test.ts runs all 5 sample Taglish complaints from PLAN.md through runTriage() with forceFinal, all pass against live OpenRouter + Supabase (7/7 npm test). Verified live in browser: sample complaint returned correct Engine Diagnostic match with real seeded price range.
Audit: 2026-07-14 — audited-pass. All 5 PLAN.md Taglish complaints (tests/triage.test.ts) return grounded probable_issue/urgency/estimate mapped to live autoshop_services or needs_inspection, all carry the exact disclaimer string; npm test 7/7 and npm run build pass live against Supabase+OpenRouter; model read from OPENROUTER_MODEL (no hardcode); no service-role/OpenRouter key in client code (/intake only calls fetch("/api/triage")); no undeclared new deps vs decision log.

## Phase 3: Booking + status tracker — audited-pass
Notes: /book (server component fetching services + client BookingForm) creates an autoshop_jobs row via new app/api/book/route.ts (service-role client, since anon has select-only RLS) plus an autoshop_status_history "booked" row. Prefillable from /intake's "Book this repair" link (service_name/probable_issue/urgency/estimate carried via query params — no re-invented numbers, reuses Phase 2's grounded triage result). Slot picker is date input + fixed hourly TIME_SLOTS. /track is a server component reading ?plate=&phone= via a plain GET form, looks up autoshop_jobs (ilike plate, eq phone) + autoshop_status_history ordered by created_at, renders a timeline. tests/booking.test.ts inserts a job+history via service client, verifies anon-client retrieval by plate+phone and timeline presence, then cleans up (3/8 new, 8/8 total npm test pass against live Supabase). npm run build passes. Verified live in browser: filled /book form → job created (confirmed via network response) → /track?plate=&phone= showed customer, job, "booked" status, and timeline entry "Booked via customer portal"; test row deleted after.
Audit: 2026-07-14 - audited-pass. Done-when verified independently: POSTed live to app/api/book/route.ts (not just direct DB insert) with a test job (plate AUD 9999), confirmed 200 + job row returned with status=booked, then loaded /track?plate=AUD 9999&phone=... and confirmed customer name, probable_issue, estimate range + "initial estimate, subject to inspection" disclaimer, and the "Booked via customer portal" timeline entry rendered; test row deleted after via service client. npm test 8/8 pass, npm run build passes clean (verified myself). RULES.md compliance: supabaseService import confined to lib/supabase/server.ts (server-only guard) and only used inside app/api/book/route.ts, a route handler, never a client component - rule 6 satisfied. Every estimate shown on /book and /track is paired with the exact "initial estimate, subject to inspection" disclaimer and is sourced only from Phase 2 triage query params, never invented - rules 8/9 satisfied. No model calls in this phase - rule 10 N/A. No sends here - rule 11 N/A. Seed script untouched - rule 12 OK. Minor non-blocking note: tests/booking.test.ts inserts the job/history directly via the service client rather than POSTing to /api/book, so it does not itself exercise route.ts request validation - this gap is covered by this audit's live curl verification of the actual route, so overall verdict stands as audited-pass.

## Phase 4: Admin board + AI messaging — audited-pass
Notes: /admin gated by ADMIN_PASSCODE via httpOnly cookie (sha256 of passcode, checked server-side in lib/admin/auth.ts + admin API routes). Kanban board (AdminBoard.tsx) shows autoshop_jobs by status (booked/in_progress/waiting_parts/ready/done from lib/statuses.ts), "Advance to X" button moves to next status via app/api/admin/jobs/[id]/status, inserting a status_history row and an AI-drafted status_update message (lib/openrouter/messages.ts draftMessage, structured {body} JSON, callWithRetry-wrapped). Draft Completion report/Reminder/Review request buttons call app/api/admin/jobs/[id]/draft for the other 3 message kinds. Drafts are editable textareas; "Send" (app/api/admin/messages/[id]/send) marks sent=true (optionally with edited body) — nothing is actually transmitted (rule 11). New RLS policy on autoshop_messages (`sent = true`) lets the public /track tracker read only sent status_update/completion_report messages, rendered under "Messages from the shop"; reminder/review_request stay demo-only previews in admin. tests/admin.test.ts calls draftMessage() live against OpenRouter, then verifies via direct DB ops that an unsent message is invisible to the anon client and a sent one is visible (9/9 npm test pass). npm run build passes clean.
Audit: 2026-07-14 — audited-pass. Independently verified all Phase 4 "Done when" criteria: (1) /admin gate: confirmed server-side only via curl — unauthenticated POST to /api/admin/jobs/:id/status returns 401, wrong passcode to /api/admin/login returns 401, correct ADMIN_PASSCODE (read from .env.local, sha256 httpOnly cookie set in lib/admin/auth.ts, verified server-side in every admin route via isAdminAuthedFromHeader) returns 200 + cookie, then authenticated requests succeed; PasscodeForm.tsx (client) only calls fetch, never checks the passcode itself. (2) Kanban + status moves: lib/statuses.ts STATUS_ORDER is booked->in_progress->waiting_parts->ready->done; live curl advanced a real seeded job (Liza Fernandez, DEF 3344) from booked to in_progress via app/api/admin/jobs/[id]/status, confirmed job.status updated and a status_history row inserted; invalid status string rejected with 400. (3) AI messaging: advancing status returned a live OpenRouter-drafted {\"body\":...} status_update message (lib/openrouter/messages.ts draftMessage, wrapped in callWithRetry, model read from process.env.OPENROUTER_MODEL never hardcoded, response parsed/validated as JSON per rule 7); app/api/admin/jobs/[id]/draft live-generated a reminder draft for the same job and correctly rejected status_update (400 Invalid kind), since that kind is only produced by the status-change flow. (4) Send + tracker surfacing: confirmed via live /track fetch that the unsent status_update was NOT rendered (Messages from the shop section absent), then POSTed to app/api/admin/messages/[id]/send, and re-fetched /track to see the exact drafted body now rendered under Messages from the shop; matches the sent = true RLS policy in supabase/schema.sql and the anon-client query in app/track/page.tsx, which also filters kind in (status_update, completion_report) so reminder/review_request never leak publicly even if marked sent. (5) Secrets: lib/supabase/server.ts and lib/openrouter/messages.ts OPENROUTER_API_KEY usage stay server-side; AdminBoard.tsx (client) only imports type MessageKind from lib/openrouter/messages.ts via import type, erased at compile time, and otherwise only calls fetch() against /api/admin/* routes. (6) npm test: 9/9 pass live (admin.test.ts, booking.test.ts, seed.test.ts x2, triage.test.ts x5); npm run build passes clean. (7) tests/admin.test.ts genuinely exercises the completion criterion end-to-end: calls draftMessage() live, inserts as unsent, asserts anon client cannot see it, flips sent=true via service client, asserts anon client can now see it with matching body, matching what I independently verified live via the running app. (8) RULES.md: rule 3 - package.json untouched this phase (git diff clean, no new deps); rule 6 - no service-role/OpenRouter key reachable from any use-client file; rule 7 - draftMessage parseBody() throws if the model does not return valid JSON with a non-empty string body; rule 10 - model read from OPENROUTER_MODEL only; rule 11 - send only flips a DB boolean, nothing transmitted anywhere; rule 12 - npm run seed re-run successfully after all live testing, still idempotent and untouched (not in git diff), demo data restored (5 jobs, test messages cascade-deleted with their parent job). No blocking issues found. Minor non-blocking notes: draftMessage uses a single system-role prompt rather than a system/user split - stylistic, not a defect; AdminBoard re-renders from the full jobs/messages array rather than incremental refetch - fine at demo scale.

## Phase 5: Polish + deploy — audited-pass
Notes: Extracted seed data/logic into lib/seed.ts (seedDatabase(), reused by scripts/seed.ts and new passcode-gated app/api/admin/reset/route.ts). Admin board gets a "Reset demo data" button (confirm dialog, full reload after success) — same isAdminAuthedFromHeader gate as other admin routes. Nav wraps (flex-wrap) for narrow screens; admin kanban already stacks to grid-cols-1 below md. Added empty states ("No services yet." / "No jobs yet." / per-column "No jobs") to /services, /jobs, and AdminBoard columns. Added loading.tsx route-segment fallbacks for /jobs, /services, /book, /track, /admin. Added README.md covering local setup, the env var table, demo reset (CLI + in-app), and Vercel deploy steps (vercel login/link/env vars/--prod). Did not run an actual Vercel deploy — needs DV's Vercel account/login, which this session can't do headlessly; README documents the exact steps.
Verified: npm run build clean; npm test 9/9 pass live (seed refactor didn't change behavior). Live-verified /api/admin/reset via curl: 401 unauthenticated, 200 + {services:10, jobs:5} with valid passcode cookie (logged in via /api/admin/login first). Verified admin board renders correctly at 375px mobile viewport (accessibility tree: kanban columns stack vertically, Reset demo data button present) via the Browser pane's read_page — screenshot/click were unreliable in that pane (renderer hang on an unrelated stray dev process already bound to port 3000 before this session), so curl was used for the actual functional check instead of a click-through.
Audit: 2026-07-14 — audited-pass with notes. Independently re-verified, not just trusted: (1) lib/seed.ts (SEED_SERVICES x10, SEED_JOBS x5, seedDatabase(supabase) upserts services by name then deletes+reinserts jobs+status_history) is a plain server-safe module (no "use client", takes an injected SupabaseClient) — confirmed scripts/seed.ts (CLI) and app/api/admin/reset/route.ts both call seedDatabase() with a service-role client constructed only in server contexts; no duplication of seed data found elsewhere. (2) app/api/admin/reset/route.ts gates via isAdminAuthedFromHeader (same helper as other admin routes, lib/admin/auth.ts, server-only via "server-only" import) before calling seedDatabase(supabaseService) — confirmed live: curl POST /api/admin/reset unauthenticated -> 401; curl POST /api/admin/login with wrong passcode -> 401; with correct ADMIN_PASSCODE (value already on record in this file's Phase 4 decision log) -> 200 + httpOnly cookie; POST /api/admin/reset with that cookie -> 200 {"ok":true,"services":10,"jobs":5}; then fetched /jobs and confirmed all 5 seeded names (Jerome Villanueva, Ana Reyes, Marco Dela Cruz, Liza Fernandez, Paolo Santos) rendered — reset is a genuine, verified round-trip, not just a 200 status. Ran `npm run seed` CLI afterward, same 10/5 counts, no errors — idempotency holds (rule 12). (3) AdminBoard.tsx "Reset demo data" button: confirm() dialog, disables while busy, POSTs to /api/admin/reset, reloads page on success, alerts on failure — matches claim; only imports `type MessageKind` (erased at compile time) and calls fetch() against /api/admin/* routes, no secret reachable client-side (rule 6 held — grepped AdminBoard.tsx/layout.tsx/services/jobs pages for SUPABASE_SERVICE_ROLE_KEY/OPENROUTER_API_KEY, no matches). (4) Responsiveness: app/layout.tsx nav has flex flex-wrap gap-x-6 gap-y-2; AdminBoard.tsx kanban is grid-cols-1 md:grid-cols-5 — read the Tailwind classes directly and they are structurally correct for narrow-viewport stacking, but I have no visual/browser rendering tool in this audit session, so this is a code-level confirmation only, not a pixel-verified one (same limitation the implementer hit). (5) Empty states: confirmed in app/services/page.tsx ("No services yet."), app/jobs/page.tsx ("No jobs yet."), and AdminBoard.tsx per-status-column ("No jobs" when a column's filtered job list is empty) — all present exactly as claimed. (6) loading.tsx: confirmed all 5 files exist (app/admin, app/book, app/jobs, app/services, app/track/loading.tsx), each a minimal server component with a short loading message — reasonable route-segment fallbacks. (7) README.md: confirmed present, covers local setup (install/env/schema/seed/dev), an env var table (6 vars, each with where-used + notes, matches .env.example and lib usage), demo reset (CLI + in-app), Vercel deploy steps (login/link/env vars/--prod), and a testing section. .env.example still present (rule 13). (8) npm run build: ran myself, compiles clean, TypeScript clean, all 14 routes generated (static + dynamic mix as expected, /api/admin/reset present). npm test: ran myself, 9/9 pass live (seed x2, triage x5, booking x1, admin x1) — matches the implementer's claim exactly. (9) rule 3 (no undeclared deps): git diff HEAD -- package.json is empty, confirmed no dependency changes this phase. (10) Deploy criterion: PLAN.md's literal Phase 5 "Done when" requires verification "against the deployed URL" on a phone-sized screen. No Vercel deploy was performed in this session or prior ones (no vercel.json/.vercel dir, README deploy section is instructions-only). My judgment: this does NOT sink the phase to audited-fail. Deploying to Vercel is an external, credentialed action (DV's own Vercel account/login) that an agent correctly should not and cannot perform headlessly — attempting to fabricate or fake a deploy would be worse than deferring it honestly. What IS in scope and verifiable by an implementer/auditor — mobile-responsive CSS, empty/loading/error states, a genuinely working reset button, and complete env/deploy documentation with zero deploy blockers (clean build, no missing env vars, no undocumented steps) — all pass. This phase is therefore audited-pass with the explicit caveat that the actual Vercel deploy + final on-device phone walkthrough against the live URL remains an open action item for DV before the first real pitch; it is not a code or process defect and does not need another engineering session, just the deploy action itself. Minor non-blocking notes: (a) app/api/admin/reset/route.ts has no test file exercising it (tests/*.test.ts has no reset test) — coverage gap, but this audit's live curl round-trip (auth-gate + restore + rendered verification) substitutes for it; (b) README's "after deploy, run npm run seed locally" step assumes the deployer's local .env.local points at the same Supabase project as the Vercel deployment — worth a one-line reminder when DV actually deploys, not a blocker now.

## Phase 6: Shop config layer — audited-pass
Notes: Added `autoshop_shops` as the active configuration backbone (shop identity, country, locale, currency, timezone, language, sender identity, address), scoped services/jobs/bookings/admin/triage to the active shop, centralized `Intl` currency/date formatting, converted shop-local booking slots to UTC using the configured timezone, and removed regional formatting literals from feature code. All app tables and clients are isolated in the dedicated `auto_repair` PostgreSQL schema so the shared Supabase project's other apps remain untouched. The live migration moved only the four existing `public.autoshop_*` tables into `auto_repair`, added the shop table/columns, preserved the project's existing exposed schemas, and appended `auto_repair` to its manual PostgREST configuration. The seed creates US and PH shop records, preserves or accepts the active shop key, and associates the existing PH price list/jobs with RapidFix; final US services/jobs remain Phase 9 scope. Verified live that changing only the active shop record switches the rendered brand from RapidFix Auto Care to Northstar Auto Service, then restored PH as active. `npm run lint`, TypeScript, production build, seed, and the full 13/13 live test suite pass. Phase 6 is built and ready for independent audit.
Audit: 2026-07-30 — audited-pass via the consolidated v2 independent audit. Findings graded P0-P3 were remediated across f56c4ce (customer data exposure, admin credential), 0537202 (transactional integrity, email idempotency), 84d3b67 (P2/P3), and 85cb84b (release hardening). See RELEASE-v2.md for lineage.

## Phase 7: i18n extraction — audited-pass
Notes: Installed `next-intl` and added explicit locale routing through `/[locale]` with a Next.js 16 `proxy.ts`; `/` redirects to `/en`, and locale-aware navigation preserves the prefix. Extracted all visible component copy, metadata, errors, empty/loading states, labels, confirmations, and button states into `messages/en.json`, with a `NextIntlClientProvider` covering client forms and the admin board. All regional currency/date/time rendering remains centralized through `Intl` and active-shop configuration. Removed Taglish UI, prompt instructions, test complaints, and seed issue descriptions. Both triage and message-drafting prompts now receive `shop.language` as a BCP 47 language parameter. OpenRouter response parsing was moved inside the retry boundary so malformed or truncated structured responses are retried. Added `tests/i18n.test.ts` to verify catalog namespaces, zero embedded user-facing JSX text nodes, and language-driven prompts. Verified `/en`, `/en/intake`, `/en/services`, `/en/jobs`, `/en/book`, `/en/track`, and `/en/admin` all return 200 with their expected catalog copy and no missing-message marker; `/` returns 307 to `/en`. The complete live Supabase/OpenRouter suite passes 16/16; production build, lint, and TypeScript all pass.
Audit: 2026-07-30 — audited-pass via the consolidated v2 independent audit. Findings graded P0-P3 were remediated across f56c4ce (customer data exposure, admin credential), 0537202 (transactional integrity, email idempotency), 84d3b67 (P2/P3), and 85cb84b (release hardening). See RELEASE-v2.md for lineage.

## Phase 8: Email + automation layer via n8n — built, activation pending
Notes: Added the private `auto_repair.autoshop_email_deliveries` log, customer email capture, idempotent reservation/completion functions, a rolling 24-hour demo send cap, and a thin `sendEmail()` abstraction selected by `EMAIL_TRANSPORT=n8n|resend`. Status changes and manual admin sends use the same abstraction; scheduled reminders query booked appointments due within 24 hours and draft/send through the same path. Added four safely rendered templates, authenticated n8n dispatch and callback endpoints, and importable inactive delivery/reminder workflows under `n8n/workflows/`. Applied the Phase 8 migration successfully to the shared Supabase project without creating objects outside the dedicated schema. Phase 8 tests cover both transport adapters, four templates, shared-secret rejection, workflow structure, private delivery RLS, idempotency, status write-back, and cap enforcement. Lint, TypeScript, production build, and the complete test suite pass. Live n8n/Resend delivery remains pending because the required webhook URL, shared secret, API key, public app origin, and safe demo recipient are not configured in `.env.local`; the active shop also still has a seeded `.example` sender that must be replaced with an address on the verified delivery domain. Workflow import/credential connection and the two real-inbox sends cannot be truthfully verified until those items are supplied.
Audit: 2026-07-30 — code audited-pass through the consolidated v2 audit, but the phase's own "Done when" criteria remain unmet: no real email has been delivered. .env.local has no EMAIL_TRANSPORT, webhook URL, shared secret, APP_BASE_URL, demo recipient, or Resend key, the active shop still uses a seeded .example sender, and both n8n workflows remain inactive exports. Status stays "built, activation pending" until two real-inbox sends are verified.

## Phase 9: US demo data, shop switcher, deploy — audited-pass
Notes: Rebuilt the seed as two complete regional demos: Northstar Auto Service has a 10-service USD catalog and five US jobs featuring an F-150, Camry, Silverado, RAV4, and Pilot; RapidFix retains its 10-service PHP catalog and five Philippines jobs. A no-argument seed and the passcode-gated in-app reset now deterministically restore all 20 services and 10 jobs with the US shop active. Added a service-role-only `set_active_autoshop()` RPC under the dedicated `auto_repair` schema and an authenticated `/api/admin/shop` route, giving the admin board an accessible, touch-friendly market switcher that changes the complete customer/admin experience without reseeding. Current US prices were sanity-checked against July 2026 AAA and RepairPal estimates, then kept as broad initial-estimate ranges subject to inspection. Applied the Phase 9 migration live. Verified the reset API returns `{shops:2, services:20, jobs:10, activeShop:"us"}`. At a 390px viewport, clicked through home, intake, services, jobs, booking, tracking, and admin for both markets with no horizontal overflow; verified real seeded tracker lookups for Sophia Bennett and Liza Fernandez. The full live suite passes 24/24, and lint, TypeScript, and the production build pass. Deployed successfully to Vercel and verified `https://auto-repair-ten.vercel.app` opens Northstar/US by default, renders USD services and US vehicles, switches to RapidFix/PHP in production, and was restored to Northstar before handoff. The design-taste quality gates influenced the compact market-control panel, selected-state clarity, keyboard focus, touch target sizing, mobile reading order, and restrained visual hierarchy.
Audit: 2026-07-30 — audited-pass via the consolidated v2 independent audit. Findings graded P0-P3 were remediated across f56c4ce (customer data exposure, admin credential), 0537202 (transactional integrity, email idempotency), 84d3b67 (P2/P3), and 85cb84b (release hardening). See RELEASE-v2.md for lineage.

## Phase 10: Technician assignment — built, audit pending
Notes: Added the private, shop-scoped `autoshop_technicians` table and nullable job assignee through uniquely versioned migration `20260731130000`. A composite `(technician_id, shop_id)` foreign key prevents cross-shop assignments at the database layer, while the service-role-only `assign_autoshop_technician()` RPC locks the target job, validates an active technician from the same shop, and supports assign, reassign, and unassign. Both markets seed four named technicians and deterministic defaults. The destructive demo reset captures valid assignments by shop and plate before recreating jobs, then restores them against stable upserted technician identities, so CLI seed and the in-app reset preserve reassigned seeded vehicles. The admin board loads only the active shop's technicians, provides an assignment control on every job card, and filters the board by all, unassigned, or one technician. The assignment API is passcode-gated, UUID-validated, bounded to 512 bytes, and derives the active shop server-side. Clean-database migration and executable SQL behavior checks passed; a real local seed → reassign → seed round trip preserved Mason Brooks's reassignment from Jordan Lee to Marcus Reed. Browser QA verified assignment to Jordan Lee, a Marcus Reed filter narrowing the board to two jobs, usable controls at desktop and 390px, zero horizontal overflow, and no console warnings/errors. The full isolated Supabase/OpenRouter suite passes 45/45; lint, TypeScript, production build, migration validation, and diff checks pass. The live Supabase project remains untouched until its documented controlled migration baseline/push is completed.
Audit: Pending independent Phase 10 audit.

## Phase 11: Extra-work approval flow — built, activation and audit pending
Notes: Built the private `autoshop_approval_requests` data model and uniquely versioned `20260731140000` migration. Staff can request extra work only for active repairs, select a service from the active shop's own price list, enter an amount inside that service's database-enforced range, and describe the finding. OpenRouter returns a validated structured explanation covering the finding and consequence of declining; the server renders the exact regional amount independently. Request, message, and delivery reservation are one transaction with client UUID idempotency, one pending request per job, and service-role-only access. Approval URLs are deterministic HMAC-signed tokens stored only as SHA-256 hashes, expire after 72 hours, accept one approve/decline decision, and use the database-backed public-endpoint rate limiter. Decisions transactionally update the request, return the job to `in_progress`, and add a unique history record. Added the no-login `/[locale]/approve/[token]` page, approve/decline states, admin pending badge and drafting form, a fifth template in both Resend and n8n paths, bounded APIs, localized copy, documentation, unit tests, and executable SQL behavior tests. All 10 migrations apply cleanly; Phase 10/11 database checks pass; the isolated Supabase/OpenRouter suite passes 49/49; lint, TypeScript, and production build pass. Local browser QA used a disposable email receiver and verified a $275 approval, a $300 decline, persisted board state, 409 reuse rejection, 404 tamper rejection, 410 expiry rejection, 429 rate limiting, correct USD display, no errors, and no horizontal overflow at 390px. The disposable receiver is not a real inbox: provider activation, verified sender configuration, a real-inbox click-through, production migration/deploy, and independent audit remain open.
Audit: Pending independent Phase 11 audit and the deferred real-inbox completion proof.

## Phase 12: Photo intake with AI vision — built, audit pending
Notes: Built the project-specific private `auto-repair-intake-photos` Storage bucket, service-role-only `autoshop_intake_photos` metadata, and transactional reserve/complete/booking-attachment RPCs in uniquely versioned migration `20260731150000`. Intake supports up to three JPEG, PNG, or WebP files at 4 MB each, validates magic bytes before storage, and protects session, upload, and delete actions with scoped HMAC capabilities, 24-hour expiry, content/idempotency hashes, and database-backed rate limiting. Unbooked photos are opportunistically purged after 24 hours; booked photos follow the job lifetime; demo reset removes Storage objects before metadata and jobs. Vision triage uses only `OPENROUTER_VISION_MODEL`, validates one to five visible findings, grounds every estimate in the active shop's catalog, and falls back to the unchanged text path with no visual findings on provider failure. Booking transfers the private intake session without query-string tokens and attaches the photos transactionally; admin renders them only through five-minute signed URLs. A generated warning-light fixture returned a visible check-engine finding and the live Engine Diagnostic range of $120–$180 with the inspection disclaimer. The browser flow then created a real local booking, confirmed the attached database row, and rendered the signed admin image. Non-image content returned 415, a 4 MB-plus upload returned 400, and anonymous bucket listing returned no objects. Responsive QA found no horizontal overflow at the 390 px viewport and no new browser errors. All 11 migrations applied from an empty local database; Phase 10/11/12 database behavior checks pass; clean `npm ci` succeeds; the full isolated suite passes 56/56 including a live simulated vision-outage fallback; lint, TypeScript, production build, and the seven-test Phase 12 suite pass. Production migration/deploy and independent audit remain open.
Audit: Pending independent Phase 12 audit.

## Phase 13: Owner dashboard — built, audit pending
Notes: Added the passcode-gated `/admin/dashboard` owner view with 7/30/90-day shop-local ranges, active-shop switching, localized currency and dates, job status mix, explicitly estimated booked-value and average-ticket ranges, intake-to-booking conversion, approval acceptance, confirmed reminders sent, and current technician workload. Charts use only CSS bars and the existing design system; no runtime dependency changed. A service-role-only dashboard RPC provides one consistent snapshot. The new `autoshop_intake_sessions` table and idempotent recorder make conversion measurable rather than inferred, and the 15-argument booking overload attaches the completed intake to the idempotent booking in the same transaction while retaining the Phase 12 overload for compatibility. Seeded jobs now span 7/30/90-day windows and include representative intake, decision, and reminder records for both regional demos. The README documents each metric's cohort and makes clear that money values are estimates, not invoices or revenue. All 12 uniquely versioned migrations apply from an empty local database; Phase 10–13 database behavior checks pass; the full suite passes 57 tests with one optional unconfigured live-vision check skipped; lint, TypeScript, and the production build pass. Browser QA confirmed the admin gate, board/dashboard navigation, US USD and Philippines PHP formatting, 7-to-90-day metric changes, the Philippines seven-day empty state, and no browser errors.
Audit: Pending independent Phase 13 audit.

## Phase 14: Branded landing page and close-out — built, production deploy and audit pending
Notes: Replaced the generic home route with a shop-branded service-counter landing page driven entirely by the active `autoshop_shops` row. The page now renders the shop name, tagline, address, phone, hours, three live service previews with regional prices, and locale-preserving paths into intake, booking, service browsing, and tracking. Migration `20260731170000` adds only the three missing brand fields (`tagline`, `phone`, and `hours`), backfills both seeded shops, requires them for every shop, and keeps all data in the existing `auto_repair` schema. The shared page contains no shop-name, country, currency, or regional branch. A code-native service-desk illustration and a light, editorial layout extend the existing design system without a new runtime dependency or remote asset. Browser QA switched between Northstar and RapidFix and confirmed each brand, phone, hours, service list, and USD/PHP price set; the primary estimate and service-booking links preserved `/en`; the 390px pass had no horizontal overflow or browser warnings/errors. All 13 migrations apply from an empty local database, the Phase 10–14 SQL behavior checks pass, and the full application suite passes 60 tests with one optional unconfigured live-vision test skipped. Lint, TypeScript, production build, and diff checks pass. Production was linked and inspected: a read-only schema dump matched the documented seven-version historical baseline, those seven already-present versions were recorded in the migration ledger, and a dry run now lists exactly the six expected pending migrations (`20260731120000` through `20260731170000`). Applying those six production migrations requires explicit deployment approval; the application deploy and deployed-URL verification therefore remain open, followed by the independent v3 audit.
Audit: Pending independent v3 audit after production close-out. Phase 11 also retains its separately documented real-inbox activation requirement.

## Decision log
- 2026-07-31: v3 plan approved (PLAN-v3.md). Scope: technician assignment, extra-work approval flow, photo intake with AI vision, owner dashboard, branded landing page. Phases 10-14.
- 2026-07-31: Phase 8 email activation is a prerequisite gate for Phase 11, not a phase of its own. Phases 10, 12, 13, and 14 do not depend on it and may proceed first.
- 2026-07-31: v2 phase statuses corrected. The consolidated independent audit and P0-P3 remediation had already run; the headers were stale. Phase 8 remains "built, activation pending" because no real email has ever been delivered.
- 2026-07-31: Spanish locale deferred by choice, not cost. The Phase 7 extraction means `messages/es.json` is a bundle addition rather than a refactor; it stays the cheapest remaining differentiator.
- 2026-07-31: v3 adds no runtime dependencies. Dashboard charts are CSS/SVG bars specifically to avoid a charting library.
- 2026-07-31: New env vars for v3 — `OPENROUTER_VISION_MODEL` (vision path kept separate so the text triage model is unchanged), `APPROVAL_TOKEN_SECRET` (distinct from `ADMIN_SESSION_SECRET`), and the intake photo storage bucket name.
- 2026-07-31: Approval links are signed, single-use, and expiring, and the `/approve/[token]` route is rate limited. This is the project's first public unauthenticated state-changing endpoint and carries disproportionate audit weight in v3.
- 2026-07-31: Intake photos are stored in a private Supabase Storage bucket with signed-URL access only, capped at 3 per intake with size and MIME limits. A retention and deletion policy must be decided before the bucket goes live.
- 2026-07-31: Dashboard monetary figures derive from estimate ranges, not invoices, and must be labeled as estimated wherever displayed.
- 2026-07-31: No part of v1, v2, or v3 has been validated against a real shop owner. Pitch the live demo during the v3 build and let real feedback reorder the remaining phases.
- 2026-07-31: Phase 10 uses four stable seeded technicians per market. Job assignment is nullable and enforced with a composite technician/shop foreign key plus a service-role-only transactional RPC. Demo resets preserve valid assignments for seeded plates instead of silently reverting technician work ownership. No runtime dependency was added.
- 2026-07-31: The user explicitly overrode Phase 11's email-activation prerequisite. Phase 11 may be built and locally verified, but it cannot be marked complete or audited-pass until a real-inbox approval email is delivered and exercised from the received message.
- 2026-07-31: Phase 11 uses a deterministic `request UUID + HMAC` token so transport retries reuse one URL and one delivery identity. The database stores only the SHA-256 token hash, enforces one pending approval per job, validates the selected shop service and amount transactionally, and records each decision with a unique history action key. No runtime dependency was added.
- 2026-07-31: Phase 12 photo retention is job-lifetime for booked photos and 24 hours for abandoned intake photos. A new signed photo session opportunistically purges expired unbooked files; the in-app/CLI demo reset deletes all private Storage objects before metadata and jobs. The bucket id is `auto-repair-intake-photos`, avoiding collision in the shared Supabase project.
- 2026-07-31: Phase 13 records completed intake sessions explicitly because request-rate logs cannot truthfully represent conversion. Dashboard ranges follow each shop's local calendar; booking/value/status cohorts use job creation, approvals use decision time, reminders use confirmed send time, and technician workload stays current rather than period-bound.
- 2026-07-31: Phase 14 treats `tagline`, `phone`, and `hours` as required shop configuration alongside the existing name and address. One shared landing component renders both markets and reads its service preview from each active shop's live catalog; no shop-specific page branch, external image asset, or runtime dependency was added.
- 2026-07-31: The production Supabase schema was dumped read-only before Phase 14 release work and matched the historical baseline. Versions `20260730080000` through `20260730140000` were recorded as already applied; a subsequent dry run lists only `20260731120000` and Phases 10–14 as pending. No pending migration was applied without explicit production-mutation approval.
- 2026-07-31: The independent v3 audit returned NO-GO with four P1 findings. Local remediation now makes approval decisions fail closed after a newer job transition, rolls capped approval reservations back before any inaccessible pending request can commit, claims photo deletion under the same intake advisory lock used by booking and removes race-prone upload cleanup, and replaces the constant admin cookie with an HMAC-signed eight-hour token whose expiry is enforced server-side. Clean migration and SQL behavior checks pass; re-audit remains required before deployment.
- 2026-07-31: P2 remediation redacts customer/job details from expired or reused approval links, excludes naturally expired approvals from the admin board so the transactional create path can expire and replace them, retains abandoned upload metadata until locked Storage cleanup confirms deletion, streams public photo bodies through an actual-byte cap, and rejects model approval prose unless it preserves the selected service name and exact formatted amount. The brittle validation assertion was replaced with whitespace-independent evidence. A durable release commit and fresh independent audit are still required before deployment.
- 2026-07-31: P3 remediation passes the active shop locale into dashboard percentage formatting, moves the Phase 14 metadata title into the locale bundle, and removes four obsolete one-time Markdown staging instruction files from release source. No production state was changed; a durable commit and independent re-audit remain required.
- 2026-07-07: Plan approved. Stack: Next.js + Supabase + OpenRouter, hosted on Vercel.
- 2026-07-07: OpenRouter default model google/gemini-2.5-flash-lite, set via OPENROUTER_MODEL env.
- 2026-07-07: No auth; admin gated by ADMIN_PASSCODE. Messages faked (previewed, not sent) for the demo.
- 2026-07-14: Tables prefixed autoshop_ (services/jobs/status_history/messages) — the Supabase project is shared with another app that already owns a `jobs` table.
- 2026-07-14: Added deps: @supabase/supabase-js (client), tsx + server-only (dev, seed script + server-only guard on service-role client). Seed reads .env.local via `node --env-file`, no dotenv dep needed.
- 2026-07-14: /services and /jobs are plain Server Components fetching via anon client — fine for phase 1 read-only lists; will need dynamic rendering once Phase 3/4 make jobs mutate.
- 2026-07-14: Added `public read sent autoshop_messages` RLS policy (`sent = true`) — applied manually via Supabase SQL editor (no DB creds available to this session for direct DDL/CLI push). Admin routes use the service-role client so this doesn't affect them.
- 2026-07-14: A temporary demo admin passcode was configured for initial verification. Its committed value was removed and the credential rotated after the v2 security audit.
- 2026-07-14: Vercel project linked (dvpntransport-4680s-projects/auto-repair), all 6 env vars set in Production+Preview, deployed to production. Live URL: https://auto-repair-ten.vercel.app — Phase 5's deploy criterion is now closed (home/services/track routes curl-verified 200 live).
- 2026-07-30: v2 plan approved (PLAN-v2.md). Scope: shop config layer, full i18n extraction, real email via n8n, US default demo shop. Phases 6-9.
- 2026-07-30: Taglish dropped; English only. Language becomes a `shops` config field so other languages remain possible without a rebuild.
- 2026-07-30: Messaging channel changed from SMS to email, delivered for real rather than previewed.
- 2026-07-30: Email routed through the existing self-hosted n8n instance over an authenticated webhook. n8n also owns maintenance reminder scheduling, avoiding Vercel cron limits.
- 2026-07-30: `sendEmail()` abstraction behind `EMAIL_TRANSPORT=n8n|resend` so transport can be swapped with one env var if n8n is unavailable mid-pitch.
- 2026-07-30: Full i18n bundles adopted while shipping `en` only — extraction is cheaper now than after more features land.
- 2026-07-30: Default demo shop changed from RapidFix Auto Care (Quezon City, PHP) to a US shop in USD; PH shop kept as a switchable second seed.
- 2026-07-30: New dependencies to install — `next-intl`, `resend`.
- 2026-07-30: Shared Supabase isolation — all AutoShop Assistant tables live under the exposed `auto_repair` PostgreSQL schema; feature code uses schema-scoped Supabase clients and leaves other apps' `public` data untouched.
- 2026-07-30: Per-phase v2 audits deferred by explicit user override; build phases 6-9 in order and run one independent audit after v2 is complete.
- 2026-07-30: Installed `next-intl` for Phase 7. Locale URLs use an explicit prefix (`/en`) because hiding the default prefix produced a redirect loop with the Next.js 16 proxy; explicit prefixes also make future locale routing deterministic.
- 2026-07-30: Installed the approved `resend` dependency for Phase 8. Both n8n and direct Resend transports use the same template payload and delivery id as the idempotency key.
- 2026-07-30: Phase 8 database objects remain confined to the `auto_repair` schema. Delivery logs are service-role only, and the shared-secret n8n callback is the only public status-write path.
- 2026-07-30: Phase 8 n8n credential connection and real-inbox activation were explicitly deferred by the user so Phase 9 could proceed; the code, schema, fallback adapter, and inactive workflows remain built and tested.
- 2026-07-30: Phase 9 US service ranges were sanity-checked against current AAA guidance and RepairPal national estimates (updated June/July 2026), including oil service, brake pads, A/C recharge, batteries, diagnostics, alignment, timing belts, suspension, tires, and transmission service.
- 2026-07-30: Phase 9 adds no dependencies. The test runner is serialized so the intentional global active-shop switch cannot race other live Supabase/OpenRouter tests.
- 2026-07-30: Phase 9 production deployment completed and retained the existing alias `https://auto-repair-ten.vercel.app`.
- 2026-07-30: P0 security hardening removed anonymous access to jobs, status history, messages, deliveries, and tracker attempts. `/jobs` now redirects to a plate-plus-phone tracker backed by a rate-limited, service-role-only minimal lookup. Admin authentication moved to a new HMAC-signed v2 cookie using `ADMIN_SESSION_SECRET`, invalidating legacy cookies; the exposed demo credential was removed and must be rotated in local/Vercel environments.
- 2026-07-30: P1 integrity hardening moved booking plus initial history into one server-authoritative database transaction, deriving shop, price, and UTC schedule from locked database configuration. Status advancement now drafts before mutation, then conditionally writes the transition, history, message, and email outbox atomically with deterministic replay protection. Both n8n and direct Resend attempts reuse the delivery ID as the provider idempotency key, and ambiguous outcomes remain reconcilable instead of being falsely finalized as failures.
- 2026-07-30: P2/P3 audit remediation repaired the Phase 6 upgrade path with legacy shop backfills and clean-install-equivalent non-null constraints; added database-backed quotas and bounded request parsing to booking, triage, and admin login; added lost-response, ambiguous-provider, callback-replay, and bundle-only locale tests; moved n8n callback authentication entirely into its credential store; upgraded Next.js and its vulnerable runtime transitive dependencies to audited patched versions; and documented the immutable V2 release lineage and rollback rules in `RELEASE-v2.md`.
- 2026-07-31: Post-audit release hardening established a real Supabase migration chain with unique 14-digit versions and a missing pre-V2 initial-schema migration. Production inspection confirmed `supabase_migrations.schema_migrations` was absent and PostgreSQL 17.6 matched the checked-in local configuration; `supabase/BASELINE.md` now defines the controlled seven-version `migration repair` baseline before the new safety migration is pushed. Booking capacity is one service bay per shop and is enforced transactionally using service-duration overlap checks plus advisory locks; client UUID idempotency keys return the original job on retry. Reconciliation now blocks duplicate reminders in both scheduling logic and the partial unique index. Added clean-reset CI and executable database behavior checks. Pinned the already-transitive `@swc/helpers` package at `0.5.23` to repair the lockfile and make `npm ci` reproducible; no other application dependency was added.
- 2026-07-31: Completed API input hardening across every remaining JSON body consumer. Public tracking, the authenticated n8n delivery callback, shop switching, job status transitions, draft creation, and message sending now use the streaming bounded JSON reader with endpoint-specific byte caps. Free-form provider IDs, provider errors, and edited message bodies have explicit character limits; enum-backed fields remain restricted to their known values. A repository-wide regression test rejects future direct `request.json()` use in API routes.
